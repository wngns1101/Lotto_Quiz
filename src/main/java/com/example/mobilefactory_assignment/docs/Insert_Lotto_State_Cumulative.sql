INSERT INTO lotto_state_cumulative (
    cumulative_count_rank1,
    cumulative_count_rank2,
    cumulative_count_rank3,
    cumulative_count_rank4,
    lotto_state_id
) VALUES (
             0,    -- cumulative_count_rank1 값 (예시)
             0,    -- cumulative_count_rank2 값 (예시)
             0,    -- cumulative_count_rank3 값 (예시)
             0,    -- cumulative_count_rank4 값 (예시)
             1       -- lotto_state_id 값 (예시)
         );